  <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom sticky-top" style="z-index: 1030;">
    <div class="container-fluid px-4">
      <a class="navbar-brand d-flex align-items-center" href="/rooms">
        <i class="bi bi-house-door fs-4 me-2"></i> TRỌ VÍP
      </a>

      <!-- Thanh tìm kiếm (Desktop) -->
      <div class="search-bar d-none d-md-block flex-grow-1 mx-4" style="max-width: 400px; position: relative;">
        <i class="bi bi-search" style="position: absolute; left: 15px; top: 50%; transform: translateY(-50%); color: #64748b;"></i>
        <input type="text" id="searchInput" class="form-control ps-5 border" placeholder="Tìm kiếm theo tiêu đề, địa chỉ...">
      </div>

      <div class="ms-auto d-flex align-items-center">
        <!-- Nút Yêu thích -->
        <button id="favNavBtn" class="btn btn-light rounded me-3 d-flex justify-content-center align-items-center border"
          style="width: 38px; height: 38px;" title="Tin đã lưu" onclick="toggleFavoritesOnly()">
          <i class="bi bi-heart-fill fs-6"></i>
        </button>

        <!-- Nút riêng theo role -->
        <% if (user && user.role === ROLES.ADMIN) { %>
          <a href="/admin" class="btn btn-outline-secondary btn-sm rounded px-3 fw-semibold me-3">
            <i class="bi bi-shield-lock me-1"></i> Admin Panel
          </a>
          <a href="/rooms/add" class="btn btn-primary btn-sm rounded px-3 fw-bold me-3" style="background-color: #1e3a8a; border-color: #1e3a8a;">
            <i class="bi bi-pencil-square me-1"></i> Đăng tin
          </a>
        <% } else if (user && user.role === ROLES.LANDLORD) { %>
          <a href="/rooms/dashboard" class="btn btn-outline-secondary btn-sm rounded px-3 fw-semibold me-3">
            <i class="bi bi-speedometer2 me-1"></i> Dashboard
          </a>
          <a href="/rooms/add" class="btn btn-primary btn-sm rounded px-3 fw-bold me-3" style="background-color: #1e3a8a; border-color: #1e3a8a;">
            <i class="bi bi-pencil-square me-1"></i> Đăng tin
          </a>
        <% } %>

        <!-- Dropdown người dùng -->
        <div class="dropdown">
          <button class="btn btn-light rounded dropdown-toggle border" type="button" data-bs-toggle="dropdown">
            <i class="bi bi-person fs-5 me-1 text-secondary align-middle"></i>
            <span class="fw-medium"><%= user ? user.username : 'Khách' %></span>
          </button>
          <ul class="dropdown-menu dropdown-menu-end shadow-sm border-0 mt-2">
            <% if (user) { %>
              <% if (user.role === ROLES.ADMIN) { %>
                <li><a class="dropdown-item" href="/admin"><i class="bi bi-shield-lock me-2 text-muted"></i> Admin Panel</a></li>
              <% } else if (user.role === ROLES.LANDLORD) { %>
                <li><a class="dropdown-item" href="/rooms/dashboard"><i class="bi bi-speedometer2 me-2 text-muted"></i> Dashboard</a></li>
              <% } %>
              <li><a class="dropdown-item" href="/users/change-password"><i class="bi bi-key me-2 text-muted"></i> Đổi mật khẩu</a></li>
              <li><hr class="dropdown-divider"></li>
            <% } %>
            <li><a class="dropdown-item text-danger" href="/users/logout"><i class="bi bi-box-arrow-left me-2"></i> Đăng xuất</a></li>
          </ul>
        </div>
      </div>
    </div>
  </nav>
