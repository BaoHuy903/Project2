<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/design-system.css">
  <link rel="stylesheet" href="/css/auth.css">
</head>
<body>

<div class="auth-center-container w-100 flex-column">
  <div class="mb-4 text-start" style="width: 100%; max-width: 480px;">
    <a href="/users/login" class="btn-tv btn-tv-outline btn-tv-sm rounded-pill">
      <i class="bi bi-arrow-left"></i> Quay lại Đăng nhập
    </a>
  </div>

  <div class="auth-card">
    <div class="text-center mb-4">
      <i class="bi bi-question-circle-fill text-primary" style="font-size: 2.5rem;"></i>
      <h2 class="auth-title mt-2">Quên mật khẩu</h2>
      <p class="auth-subtitle">Khôi phục mật khẩu tài khoản của bạn</p>
    </div>

    <!-- Alerts -->
    <% if (error) { %>
      <div class="alert alert-danger border-0 rounded-3 d-flex align-items-center mb-4" role="alert" style="background-color: var(--color-danger-bg); color: var(--color-danger-text);">
        <i class="bi bi-exclamation-circle-fill me-2 fs-5"></i>
        <div class="small fw-medium"><%= error %></div>
      </div>
    <% } %>

    <% if (success) { %>
      <div class="alert alert-success border-0 rounded-3 d-flex align-items-center mb-4" role="alert" style="background-color: var(--color-success-bg); color: var(--color-success-text);">
        <i class="bi bi-check-circle-fill me-2 fs-5"></i>
        <div class="small fw-medium"><%= success %></div>
      </div>
    <% } %>

    <form id="forgotForm" method="POST" action="/users/forgot-password" class="needs-validation" novalidate>
      <!-- Username -->
      <div class="mb-3">
        <label class="form-label fw-semibold text-secondary small mb-2">Tên tài khoản</label>
        <div class="auth-input-wrapper">
          <i class="bi bi-person-fill input-icon"></i>
          <input type="text" id="username" name="username" class="input-tv w-100" placeholder="Nhập tên tài khoản..." required autocomplete="username">
          <div class="invalid-feedback text-danger small mt-1">Vui lòng nhập tên tài khoản cần khôi phục.</div>
        </div>
      </div>

      <!-- New Password -->
      <div class="mb-2">
        <label class="form-label fw-semibold text-secondary small mb-2">Mật khẩu mới</label>
        <div class="auth-input-wrapper">
          <i class="bi bi-key-fill input-icon"></i>
          <input type="password" id="password" name="newPassword" class="input-tv w-100" placeholder="Nhập mật khẩu mới..." required autocomplete="new-password">
          <i id="eyeIconNew" class="bi bi-eye eye-toggle" onclick="togglePassword('password', 'eyeIconNew')"></i>
          <div class="invalid-feedback text-danger small mt-1">Mật khẩu mới không hợp lệ.</div>
        </div>
      </div>

      <!-- Password Requirements -->
      <ul class="req-list" id="passwordReqs">
        <li id="reqLength" class="invalid"><i class="bi bi-x-circle-fill"></i> Tối thiểu 6 ký tự</li>
        <li id="reqAlphaNum" class="invalid"><i class="bi bi-x-circle-fill"></i> Chứa cả chữ cái và chữ số</li>
      </ul>

      <!-- Confirm Password -->
      <div class="mb-4">
        <label class="form-label fw-semibold text-secondary small mb-2">Xác nhận mật khẩu mới</label>
        <div class="auth-input-wrapper">
          <i class="bi bi-patch-check-fill input-icon"></i>
          <input type="password" id="confirmPassword" name="confirmPassword" class="input-tv w-100" placeholder="Nhập lại mật khẩu mới..." required autocomplete="new-password">
          <i id="eyeIconConfirm" class="bi bi-eye eye-toggle" onclick="togglePassword('confirmPassword', 'eyeIconConfirm')"></i>
          <div class="invalid-feedback text-danger small mt-1" id="confirmFeedback">Xác nhận mật khẩu mới không khớp.</div>
        </div>
      </div>

      <!-- Button -->
      <div class="d-grid">
        <button type="submit" class="btn-tv btn-tv-primary btn-tv-lg w-100" id="btnSubmit">
          Khôi phục mật khẩu <i class="bi bi-arrow-right ms-2"></i>
        </button>
      </div>
    </form>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
  // Show / Hide password
  function togglePassword(inputId, iconId) {
    const input = document.getElementById(inputId);
    const icon = document.getElementById(iconId);
    if (input.type === "password") {
      input.type = "text";
      icon.classList.remove("bi-eye");
      icon.classList.add("bi-eye-slash");
    } else {
      input.type = "password";
      icon.classList.remove("bi-eye-slash");
      icon.classList.add("bi-eye");
    }
  }

  // Real-time Password Strength Check
  const passwordInput = document.getElementById('password');
  const reqLength = document.getElementById('reqLength');
  const reqAlphaNum = document.getElementById('reqAlphaNum');

  passwordInput.addEventListener('input', () => {
    const val = passwordInput.value;
    const hasLetter = /[a-zA-Z]/.test(val);
    const hasNumber = /[0-9]/.test(val);
    
    // Check length
    if (val.length >= 6) {
      reqLength.classList.remove('invalid');
      reqLength.classList.add('valid');
      reqLength.querySelector('i').className = 'bi bi-check-circle-fill';
    } else {
      reqLength.classList.remove('valid');
      reqLength.classList.add('invalid');
      reqLength.querySelector('i').className = 'bi bi-x-circle-fill';
    }

    // Check letter & number
    if (hasLetter && hasNumber) {
      reqAlphaNum.classList.remove('invalid');
      reqAlphaNum.classList.add('valid');
      reqAlphaNum.querySelector('i').className = 'bi bi-check-circle-fill';
    } else {
      reqAlphaNum.classList.remove('valid');
      reqAlphaNum.classList.add('invalid');
      reqAlphaNum.querySelector('i').className = 'bi bi-x-circle-fill';
    }

    // Set validity
    if (val.length >= 6 && hasLetter && hasNumber) {
      passwordInput.setCustomValidity('');
    } else {
      passwordInput.setCustomValidity('Weak password');
    }
  });

  // Real-time match validation
  const confirmInput = document.getElementById('confirmPassword');
  const confirmFeedback = document.getElementById('confirmFeedback');
  
  function validateConfirm() {
    if (confirmInput.value !== passwordInput.value) {
      confirmInput.setCustomValidity('Mismatch');
      confirmFeedback.textContent = 'Xác nhận mật khẩu mới không khớp.';
    } else {
      confirmInput.setCustomValidity('');
    }
  }
  
  confirmInput.addEventListener('input', validateConfirm);
  passwordInput.addEventListener('input', validateConfirm);

  // Client-side validation bootstrap
  (() => {
    'use strict'
    const forms = document.querySelectorAll('.needs-validation')
    Array.from(forms).forEach(form => {
      form.addEventListener('submit', event => {
        // Trigger manual validate checks
        passwordInput.dispatchEvent(new Event('input'));
        confirmInput.dispatchEvent(new Event('input'));

        if (!form.checkValidity()) {
          event.preventDefault()
          event.stopPropagation()
        }
        form.classList.add('was-validated')
      }, false)
    })
  })()
</script>
</body>
</html>
